# Python
python/.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Playwright persistent profile / auth (live session — never commit)
python/.pw-profile/
python/.auth/

# Rust
rust/target/

# Operational job input (may contain trainee data)
job.json
rust/job.json

# Batch checkpoint (trainee ids/names — same sensitivity as the job file)
dssp.checkpoint.json
rust/dssp.checkpoint.json

# Env
.env
.env.local

# OS
.DS_Store
node_modules/
dist/
